/* ===== Base / Tokens ===== */
:root{
  --bg: #0b0d10;
  --panel: #131720;
  --text: #e7edf3;
  --muted: #a8b3c7;
  --brand: #5aa9ff;
  --brand-2: #7cf4c6;
  --border: #273241;
  --shadow: 0 10px 30px rgba(0,0,0,.35);
  --radius: 16px;
}

* { box-sizing: border-box; }
html { scroll-behavior: smooth; }
body{
  margin:0;
  font: 400 16px/1.65 system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, "Apple Color Emoji","Segoe UI Emoji";
  color: var(--text);
  background: radial-gradient(1200px 800px at 20% -10%, rgba(90,169,255,.12), transparent 50%),
              radial-gradient(1000px 700px at 120% 10%, rgba(124,244,198,.10), transparent 40%),
              var(--bg);
}

/* ===== Layout ===== */
main, header, footer{
  width: min(100%, 1000px);
  margin-inline: auto;
  padding-inline: clamp(16px, 4vw, 28px);
}
header{ padding-block: 32px 8px; }
main{  padding-block: 8px 48px; }
footer{ padding-block: 32px 48px; color: var(--muted); }

/* ===== Skip-Link ===== */
a[href="#main"]{
  position:absolute; left:-9999px; top:auto; width:1px; height:1px; overflow:hidden;
}
a[href="#main"]:focus{
  position: fixed; left: 16px; top: 16px; width:auto; height:auto; padding:10px 14px;
  background: var(--panel); color: var(--text); border:1px solid var(--border); border-radius: 10px;
  box-shadow: var(--shadow);
}

/* ===== Typography ===== */
h1{ font-size: clamp(28px, 4vw, 44px); line-height:1.15; margin: 8px 0 4px; }
h2{ font-size: clamp(20px, 3vw, 28px); margin: 28px 0 12px; }
h3{ font-size: clamp(18px, 2.4vw, 22px); margin: 18px 0 6px; }
p, li{ color: var(--text); }
small, .muted{ color: var(--muted); }

/* Subheadline unter H1 (dein „Student · Web Engineering · AI-Interessen“) */
h1 + p{
  margin-top: 6px;
  color: var(--muted);
}

/* ===== Links & Lists ===== */
a{
  color: var(--brand);
  text-decoration: none;
}
a:hover{ text-decoration: underline; }
ul{ padding-left: 18px; }
nav ul{
  list-style: none; padding: 0; margin: 16px 0 0;
  display: flex; flex-wrap: wrap; gap: 12px 18px;
}
nav a{
  display:inline-block; padding:8px 12px;
  background: linear-gradient(180deg, rgba(90,169,255,.12), transparent);
  border:1px solid var(--border); border-radius: 999px;
}
nav a:hover{
  background: linear-gradient(180deg, rgba(90,169,255,.18), transparent);
}

/* ===== Sections as Cards ===== */
section{
  background: color-mix(in srgb, var(--panel) 92%, black 8%);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  box-shadow: var(--shadow);
  padding: clamp(18px, 3.5vw, 28px);
  margin-block: 18px;
}

/* Projekte: leichte Trennung der Einträge */
section h3 + p{ margin-top: 6px; }
section h3{ position: relative; padding-left: 10px; }
section h3::before{
  content: ""; position:absolute; left:0; top:.4em; width:4px; height:1.1em;
  background: linear-gradient(180deg, var(--brand), var(--brand-2));
  border-radius: 2px;
}

/* ===== Contact list inline ===== */
section#kontakt ul, section:has(a[href^="mailto:"]) ul{
  list-style:none; padding:0; margin:8px 0 0; display:flex; flex-wrap:wrap; gap:10px 14px;
}

/* ===== Footer ===== */
footer{ border-top:1px solid var(--border); text-align:center; }

/* ===== Media / Responsiveness ===== */
@media (max-width: 720px){
  nav ul{ gap: 8px 10px; }
  nav a{ padding:7px 10px; }
  section{ padding: 16px; }
}

/* ===== Optional: Light mode if user prefers ===== */
@media (prefers-color-scheme: light){
  :root{
    --bg:#f6f8fb; --panel:#ffffff; --text:#0d1117; --muted:#5b6b80; --border:#e5eaf1;
  }
  body{ background: radial-gradient(900px 600px at 20% -10%, rgba(90,169,255,.15), transparent 50%),
                 radial-gradient(700px 500px at 110% 10%, rgba(124,244,198,.14), transparent 40%),
                 var(--bg); }
}
